# Production Environment Configuration
# Used when running: npm run build

# Production API URL
# Use relative URL if API is on same server (recommended)
VITE_API_URL=/api

# If API is on same server, use relative URL:
# VITE_API_URL=/api
# Shared Auth Service API URL (Funtime-Shared API)
VITE_SHARED_AUTH_URL=https://shared.funtimepb.com/api

# Shared Auth UI URL (for redirect-based auth flow)
VITE_SHARED_AUTH_UI_URL=https://shared.funtimepb.com
